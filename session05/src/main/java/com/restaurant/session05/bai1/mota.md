## Lỗi sai 1
- Dùng `.jsp` cho Thymeleaf
- Thymeleaf chỉ xử lý `.html`
`.jsp` là của JSP engine, không phải Thymeleaf

## Lỗi sai 2
- Thiếu dấu `/` ở cuối → dễ gặp lỗi ghép path sai (`/WEB-INF/viewshome.jsp`).
